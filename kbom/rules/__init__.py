"""Domain rule engine.

Rules are the deterministic layer between AI extraction and the customer's
Excel template. Each rule has a citation (LH catalog page or round-trip
finding) so the cell inspector can show 'this row exists because of rule X'.
"""
